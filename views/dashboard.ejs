<!DOCTYPE html>
<html lang="en" dir="ltr">
<head>
  <meta charset="utf-8">
  <title><%= user.name %></title>
  <link rel="stylesheet" href="/css/dashboard.css">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Satisfy&display=swap" rel="stylesheet">
  <link href="https://fonts.googleapis.com/css2?family=Work+Sans:wght@300&display=swap" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
</head>
<body>
  <nav id="navbar" class="navbar sticky-top navbar-light bg-light">
    <div class="container-fluid">
      <a class="navbar-brand" href="/"><img id="logoImg" src="/images/logo.png" alt="logo" style="width: 200px;"></a>
      <span class="navbar-text">
        <a class="account Link" href="/">Browse</a>
        <a class="logout Link" href="/logout">Logout</a>
      </span>
    </div>
  </nav>
  <div class="options">
      <div class="option active">
        <a href="/dashboard/<%= user.id %>">Shared Items</a>
      </div>
      <div class="option">
        <a href="/dashboard/borrowed/<%= user.id %>">Borrowed Items</a>
      </div>
      <div class="option">
        <a href="/dashboard/requested/<%= user.id %>">Requested Items</a>
      </div>
      <div class="option">
        <a href="/dashboard/requests/pending/<%= user.id %>">Pending Requests</a>
      </div>
      <div class="option">
        <a href="/dashboard/requests/approved/<%= user.id %>">Approved Requests</a>
      </div>
    </div>
    <div class="row">
        <% for(let i=0;i<item.length;i++){%>
          <% if(item[i]!=null){ %>
        <div class="col-lg-4 col-md-6 col-xl-3 col-sm-12">
          <div class="card" style="width: 19rem;">
            <img src="data:image/<%=item[i].img.contentType%>;base64,<%=item[i].img.data.toString('base64')%>" style= "height: 200px" class="card-img-top" alt="thumbnail">
            <div class="card-body">
              <h4 class="card-title"><%=item[i].name%></h4>
              <p class="card-text">Category : <%=item[i].category%></p>
              <a class="btn cost"><%=item[i].cost%></a>
              <a href="/view/<%= user.id %>/<%=item[i].id%>" class="btn view">View Item</a>
              <a onclick=" return confirm('Are you sure you want to permanently remove one item?')" href="/delete/<%=item[i].id%>" class="btn delete"><b>DELETE ITEM</b></a>
            </div>
          </div>
        </div>
        <% } %>
      <%}%>
        <div class="col-lg-4 col-md-6 col-xl-3 col-sm-12">
          <a href="/addItem/<%= user.id %>" class="card" style="width: 19rem;">
            <img src="/images/add.png" class="card-img-top addNew" alt="thumbnail">
            <h4 class="card-title add-title">ADD NEW</h3>
          </a>
        </div>
    </div>
</body>
</html>
